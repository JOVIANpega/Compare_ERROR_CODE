[1mdiff --git a/main.py b/main.py[m
[1mindex a6089c2..b66f3b4 100644[m
[1m--- a/main.py[m
[1m+++ b/main.py[m
[36m@@ -25,6 +25,7 @@[m [mlogging.basicConfig([m
 logger = logging.getLogger(__name__)[m
 [m
 class ErrorCodeComparer:[m
[32m+[m[32m    """主流程控制類別，負責整合UI、Excel處理、設定管理等"""[m
     def __init__(self):[m
         # 初始化設定管理器[m
         self.config_manager = ConfigManager()[m
[36m@@ -50,7 +51,7 @@[m [mclass ErrorCodeComparer:[m
         logger.info("程式初始化完成")[m
 [m
     def compare_files(self):[m
[31m-        """比對檔案（改為背景執行緒）"""[m
[32m+[m[32m        """比對檔案（在背景執行緒執行，避免UI卡住）"""[m
         def do_compare():[m
             try:[m
                 # 檢查必要檔案是否已選擇[m
[36m@@ -125,7 +126,7 @@[m [mclass ErrorCodeComparer:[m
                             self.config_manager.get('CancelMsg')[m
                         )[m
                         return[m
[31m-                # 儲存結果[m
[32m+[m[32m                # 儲存結果（含反白）[m
                 if self.excel_handler.save_result([m
                     df_merge,[m
                     pd.read_excel(self.ui_manager.excel1_path, sheet_name="Test Item All"),[m
[36m@@ -134,7 +135,7 @@[m [mclass ErrorCodeComparer:[m
                 ):[m
                     self.ui_manager.show_info([m
                         self.config_manager.get('SuccessTitle'),[m
[31m-                        f"{self.config_manager.get('SuccessMsg')} {output_path}"[m
[32m+[m[32m                        f"{self.config_manager.get('SuccessMsg')}\n{output_path}"[m
                     )[m
                     # 更新最後使用的輸出目錄[m
                     self.config_manager.update_last_paths([m
[36m@@ -153,7 +154,7 @@[m [mclass ErrorCodeComparer:[m
                 )[m
             finally:[m
                 self.root.config(cursor="")[m
[31m-        # 執行時顯示處理中[m
[32m+[m[32m        # 執行時顯示處理中游標[m
         self.root.config(cursor="wait")[m
         threading.Thread(target=do_compare, daemon=True).start()[m
 [m
[36m@@ -163,7 +164,7 @@[m [mclass ErrorCodeComparer:[m
         self.ui_manager.update_sheet_list(sheets)[m
 [m
     def run(self):[m
[31m-        """執行程式"""[m
[32m+[m[32m        """啟動主視窗事件迴圈"""[m
         try:[m
             self.root.mainloop()[m
         except Exception as e:[m
[36m@@ -171,5 +172,6 @@[m [mclass ErrorCodeComparer:[m
             raise[m
 [m
 if __name__ == "__main__":[m
[32m+[m[32m    # 程式進入點[m
     app = ErrorCodeComparer()[m
     app.run() [m
\ No newline at end of file[m
